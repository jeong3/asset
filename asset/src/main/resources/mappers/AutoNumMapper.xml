<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
	"-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="springBootMVCAsset.mapper.AutoNumMapper">
<select id="AutoNumSelect" resultType="String">
select concat(#{sep} , nvl(max(substr( ${columnName} , #{len} )),100000)+ 1) from ${tableName}
</select>

<delete id="numsDelete">
delete from ${tableName} where ${columnName} in
<foreach collection="nums" item="num" open="(" close=")" separator="," index="index">
#{num}
</foreach>

</delete>

</mapper> 